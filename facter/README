Facter is a library for collecting facts about your system. These facts are
mostly strings (i.e., not numbers), and are things like the output of uname,
public ssh and cfengine keys, the number of processors, etc.

This package requires ruby1.8, available from sid77.slackware.it.
